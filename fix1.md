# شرح التنفيذ

| # | المهمة | شرح التنفيذ |
|---|---|---|
| 1 | رقم الموبايل دولي | **الآن:** حقل الرقم في تسجيل الدخول واستعادة كلمة المرور يقبل فقط رقمًا يبدأ بـ 0 وطوله 10 أرقام، فمن سجل برقم دولي لا يستطيع الدخول.<br>**المطلوب:** قبول كل الصيغ مثل +963958953288 و 00963958953288 و 0958953288 وأي رقم دولي، وإرساله للخادم كما كتب. التعديل في `app_regex.dart`. |
| 2 | النصوص حسب لغة التطبيق | **الآن:** عنوان ووصف الدرس والبث المباشر يصلان باللغتين (`"ar": "...", "en": "..."`) والتطبيق يعرضهما كما هما في الكارد والتفاصيل.<br>**المطلوب:** عرض النص حسب لغة التطبيق، وإن لم يوجد الإنجليزي يُعرض العربي. التعديل في `lesson_model.dart` و `lesson_detail_model.dart` و `live_session_model.dart`. |
| 3 | عنوان البث في الرئيسية | **الآن:** العنوان مكتوب نصًا ثابتًا `session.title` في `live_session_card.dart`.<br>**المطلوب:** عرض عنوان البث الحقيقي. |
| 4 | درس الفيديو: ملف أو رابط يوتيوب | **الآن:** الدرس المرئي إما ملف مرفوع أو رابط يوتيوب. أضيف حقلان إلى رد `POST /user/content/{id}/access-token`:<br>• `source`: ملف أو `youtube`.<br>• `youtube_url`: رابط اليوتيوب، و `null` للملف.<br>مثال: `{"token": "...", "expires_at": "...", "source": "youtube", "youtube_url": "https://www.youtube.com/watch?v=..."}`<br>**المطلوب:** إذا كان `source = file` يُشغّل `/media/{token}` كما الآن، وإذا كان `youtube` يُشغّل `youtube_url` بمشغل اليوتيوب الموجود في التطبيق. ويجب أن يرسل مشغل اليوتيوب التقدم مثل مشغل الملفات. |
| 5 | تكرار طلب التقدم عند الفشل | **الآن:** عند فشل `POST /user/content/{id}/progress` بـ 403 أو 404 يعيد التطبيق الطلب كل ثانية حتى يعود الطلب بنجاح للطلب.<br>**المطلوب:** إيقاف الإرسال وإظهار رسالة الخادم مرة واحدة. |